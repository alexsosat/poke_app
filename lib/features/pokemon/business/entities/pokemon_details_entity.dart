import "pokemon_entity.dart";

/// Entity that contains the PokemonDetails values.
class PokemonDetailsEntity extends PokemonEntity {
  /// Entity that contains the PokemonDetails values.
  const PokemonDetailsEntity({required super.id, required super.name});
}
